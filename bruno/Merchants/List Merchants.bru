meta {
  name: List Merchants
  type: http
  seq: 1
}

get {
  url: {{api_base}}/merchants
}

tests {
  test("should return merchants list", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.have.property("merchants");
    expect(res.getBody().merchants).to.be.an("array");
  });
}