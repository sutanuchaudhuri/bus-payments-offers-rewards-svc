meta {
  name: Get Profile History
  type: http
  seq: 1
}

get {
  url: {{api_base}}/profile-history/customer/1
}

tests {
  test("should return profile history", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.have.property("history");
    expect(res.getBody().history).to.be.an("array");
  });
}